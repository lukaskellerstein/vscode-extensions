# Goal

Implement VSCode extension and AI agent. AI agent will use MCP server (via stdio) with tools (ex. draw circle, draw rectangle, get all drawn elements ... etc.), and the MCP server will communicate with "LukasEditor" (same editor as in lukas-extension-4).
